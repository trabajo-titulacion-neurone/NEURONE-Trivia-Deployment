// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: true,
    serverRoot: 'https://neurone-trainer.lat/',
    apiURL: 'https://neurone-trainer.lat/api/',
    frontURL: 'https://neurone-trainer.lat/',
    triviaSource: 'https://neurone-trivia.lat/',
    triviaApi: 'https://neurone-trivia.lat/api/',
    adventureSource: 'https://adventure.neurone.info/',
    adventureApi: 'https://adventure.neurone.info/api/',
    assistantApi: 'http://va.neurone.info/api/',
    locale: 'es-CL'
  };
  
  /*
      * For easier debugging in development mode, you can import the following file
      * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
      *
      * This import should be commented out in production mode because it will have a negative impact
      * on performance if an error is thrown.
      */
  // import 'zone.js/dist/zone-error';  // Included with Angular CLI.  